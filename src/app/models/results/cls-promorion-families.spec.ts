import { ClsPromorionFamilies } from './cls-promorion-families';

describe('ClsPromorionFamilies', () => {
  it('should create an instance', () => {
    expect(new ClsPromorionFamilies()).toBeTruthy();
  });
});
