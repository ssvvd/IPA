import { InputParameterlist } from './inputparameterlist';

describe('Inputparameterlist', () => {
  it('should create an instance', () => {
    expect(new InputParameterlist()).toBeTruthy();
  });
});
