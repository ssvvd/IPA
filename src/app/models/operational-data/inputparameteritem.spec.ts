import { InputParameterItem } from './inputparameteritem';

describe('Inputparameteritem', () => {
  it('should create an instance', () => {
    expect(new InputParameterItem()).toBeTruthy();
  });
});
