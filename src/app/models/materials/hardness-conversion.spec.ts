import { HardnessConversion } from './hardness-conversion';

describe('HardnessConversion', () => {
  it('should create an instance', () => {
    expect(new HardnessConversion()).toBeTruthy();
  });
});
